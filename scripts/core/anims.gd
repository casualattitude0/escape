class_name Anim
extends RefCounted

## Animation names in player_frames.tres (generated by
## tools/generate_player_frames.py). Referencing these avoids typos and makes
## renames greppable.

const IDLE := "idle"
const WALK := "walk"          # speed-tier: low ground speed (visual only)
const RUN := "run"
const SPRINT := "sprint"      # speed-tier: near top speed (visual only)
const JUMP := "jump"
const JUMP_APEX := "jump_apex"  # 3-stage jump: rise -> apex -> fall
const FALL := "fall"
const LAND := "land"          # brief landing hold (drives squash + dust)
const RUN_STOP := "run_stop"  # run/sprint decelerating into idle
const IDLE_IN := "idle_in"    # transition into idle
const SLIDE := "slide"
const CRAWL := "crawl"
const CROUCH := "crouch"
const ROLL := "roll"          # tunnel exit-stun tumble (Runner)
const ATTACK := "attack"      # Runner's kill swing
const SABOTAGE := "sabotage"  # Runner mashing a device (GDD 4.1)
const SLAM := "slam"          # a device finishes breaking
const KNOCK := "knock"        # Hunter's knock swing (GDD 4.6)
const HIT := "hit"            # Runner reacting to a knock landing
const STUNNED := "stunned"    # Runner stunned by the third knock
const KNOCKBACK := "knockback"  # Runner kicked away from the device by a knock
const DIE := "die"            # Hunter killed
